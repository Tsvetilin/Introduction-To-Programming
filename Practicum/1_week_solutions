#include <iostream>

int main()
{   
    //1

    /*int n = 0;
    std::cout << "Introduce a number: \n";
    std::cin >> n;

    int modul = n % 2;

    std::cout << (modul == 0);*/

    //2

    /*int num1 = 0;
    int num2 = 0;

    std::cin >> num1;
    std::cin >> num2;

    int division = num1 / num2;
    int modul = num1 % num2;

    std::cout << division << '\n' << modul;*/

    //3

    /*int temperatureC = 0;
    int temperatureF = 0;

    std::cout << "Temperature in C: ";
    std::cin >> temperatureC;
    
    temperatureF = (9.0 / 5) * temperatureC + 32;
    std::cout << temperatureF;*/

    //4

    /*float height = 0;
    float width = 0;

    std::cout << "Height: ";
    std::cin >> height;
    std::cout << "Width: ";
    std::cin >> width;

    float perimeter = 2 * (height + width);
    float area = height * width;

    std::cout << perimeter << '\n' << area;*/

    //5

    /*int num = 0;
    std::cout << "Number: ";
    std::cin >> num;
    
    int days = num / 86400;
    int hours = (num - days * 86400) / 3600;
    int minutes = (num - (days * 86400) - (hours * 3600)) / 60 ;
    int seconds = num - (days * 86400) - (hours * 3600) - (minutes * 60);
    std::cout << days << " days, " << hours << " hours, " << minutes << " minutes and " << seconds << " seconds";*/

    //6

    /*int num = 0;
    std::cout << "Number: ";
    std::cin >> num;

    std::cout << num / 10;*/

    //7

    /*int grade = 0;

    std::cout << "Number: ";
    std::cin >> grade;

    std::cout << (grade >= 2 && grade <= 6);*/

    //8

    /*int a = 0, b = 0, c = 0, d = 0;

    std::cout << "Introduce 4 numbers: ";
    std::cin >> a >> b >> c >>d;

    std::cout << ((a >= c && a <= d) || (b >= c && b <= d)) || ((c >= a && c <= b) || (d >= a && d <= b));*/

    //9

    /*int num = 0;
    std::cout << "Introduce a number: ";
    std::cin >> num;

    int sum = num * (num + 1) / 2;
    std:: cout << sum;*/

    //10

    /*int num1, num2, num3, num4;

    std::cout << "Introduce 4 numbers:";
    std::cin >> num1 >> num2 >> num3 >> num4;

    int x = num2 / num1;

    std::cout << (num3 / num2 == x && num4 / num3 == x);*/

    //11

    /*double num1, num2;
    std::cout << "Introduce 2 numbers: ";
    std::cin >> num1 >> num2;

    num1 = num1 + num2;
    num2 = num1 - num2;
    num1 = num1 - num2;

    std::cout << num1 << '\n' << num2;*/

    //12

    /*float a, b;
    std::cout << "Introduce 2 numbers: ";
    std::cin >> a >> b;

    float biggerNum = (a > b)? a:b;

    std::cout << biggerNum;*/

    //13

    /*int num;

    std::cout << "Introduce a number: ";
    std::cin >> num;

    int aux = (num / 10 < 10 && num / 10 > 0)? num:num%10;

    std::cout << aux;*/

    //14

    /*int a, b, c, d, e;

    std::cout << "Introduce 5 numbers: ";
    std::cin >> a >> b >> c >> d >> e;

    int sumTotal = (a % 2 != 0)? a:0;
    sumTotal += (b % 2 != 0)? b:0;
    sumTotal += (c % 2 != 0)? c:0;
    sumTotal += (d % 2 != 0)? d:0;
    sumTotal += (e % 2 != 0)? e:0;

    std::cout << sumTotal;*/

    //15

    int a, b, c;
    
    std::cout << "Introduce a number: ";
    std::cin >> a >> b >> c;

    std::cout << ((a * a) + (b * b) == (c * c))? 1:0;

    return 0;
}
